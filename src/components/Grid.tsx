import React, {ReactNode} from "react";

export const Wrapper = ({children}: { children?: ReactNode }) => <div>{children}</div>
export const Grid = ({children}: { children?: ReactNode }) => <div>{children}</div>
export const Column = ({children}: { children?: ReactNode }) => <div>{children}</div>